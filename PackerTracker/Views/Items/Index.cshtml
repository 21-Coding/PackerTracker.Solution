@using PackerTracker.Models;

<h1>Current Inventory: </h1>

@if (Model.Count == 0)
{
  <p>There are no cars on the lot</p>
}
<ul>
  @foreach (CampingItems item in Model)
  {
   <li><a href="/@item.Id"> @item.Item </a></li>

  }
</ul>
<a href="/items">Add a new camping item!</a>